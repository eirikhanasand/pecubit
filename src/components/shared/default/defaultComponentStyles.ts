import { StyleSheet } from "react-native"

export const DefaultStyles = StyleSheet.create({
    sectionTitle: {
        fontSize: 24,
        fontWeight: '700',
        marginBottom: 8,
    },
    sectionDescription: {
        fontSize: 18,
        fontWeight: '400'
    },
    cardView: {
        borderRadius: 10,
        padding: 10,
        margin: 12,
        marginTop: 6,
    }
})
